//! Folder hierarchy traversal (config merge, ignores, path inference).
//!
//! Stub — see RFC-0001 and `pack` crate for the active critical path.

#![allow(dead_code)]

#[cfg(test)]
mod tests {
    #[test]
    fn stub() {}
}
